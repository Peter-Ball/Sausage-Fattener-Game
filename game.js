class game {

  constructor(file, audio, range_count){
    this.range_count = range_count;
    this.radii = new Array(this.range_count)

    for (var i = 0; i < this.range_count; i ++) {
      radii[i] = new Array(this.range_count)
    }
  }

  update() {
    for (var i = 0; i < this.range_count; i++) {
      this.radii[i] = 
    }
  }
}